# PICACG 客户端开发任务清单

> 按你的要求，先完成任务拆解，不立即进入编码实现。

## 0. 项目目标确认
- [x] 明确范围：实现一个可用的 PICACG Android 客户端（非官方）。
- [x] 技术硬约束：MD3 风格界面、OkHttp3、兼容 Android 7~16（API 24~36）。
- [x] 参考资料：`README.md` 中 API 说明 + `picacg_orginal` 反编译代码 + 可参考开源阅读器实现。

## 1. 需求分析与文档梳理
- [x] 通读 `README.md`，提取并整理 API：鉴权、用户、分类、搜索、漫画详情、章节、图片流。
- [x] 审阅 `picacg_orginal`，梳理关键业务流程（登录、书架、阅读记录、下载/缓存策略等）。
- [x] 形成《接口与数据模型映射表》（端点、请求参数、响应字段、错误码）。
- [x] 标记 API 中潜在不稳定点（签名、Header、时间戳、版本字段、限流）。

## 2. 技术方案设计
- [x] 确认架构：分层（data/domain/ui）+ MVVM + Repository。
- [x] 网络层设计：OkHttp3 拦截器链（公共头、签名、日志、重试、错误转换）。
- [x] 序列化与并发：Kotlinx Serialization/Moshi（二选一）+ Coroutines + Flow。
- [x] 本地存储：Room（历史、收藏、缓存元数据、阅读进度）。
- [x] 图片加载：评估 Coil/Glide（阅读长图、预加载、内存策略）。
- [x] 兼容策略：Android 7~16 的权限、存储、网络安全与后台限制适配。

## 3. UI/UX 方案（Material Design 3）
- [x] 定义全局主题：颜色、Typography、Shape、动态色兼容策略。
- [x] 规划页面：启动/登录、首页、分类、搜索、漫画详情、章节列表、阅读器、个人中心。
- [x] 制定阅读器交互：翻页模式（上下滑/左右翻）、沉浸式、亮度/方向/缩放控制。
- [x] 制定组件规范：TopAppBar、NavigationBar、Card、FAB、FilterChip、Snackbar 等 MD3 组件落地。

## 4. 模块化实施计划（编码阶段）
- [x] 初始化工程配置与依赖（Gradle、Kotlin、AndroidX、OkHttp3、图片库、Room）。
- [x] 搭建网络基础设施（API Client、拦截器、错误处理、Result 封装）。
- [x] 实现认证流程（登录、token 持久化、自动续期/失效处理）。
- [ ] 实现核心业务：首页/推荐、分类、搜索、详情、章节、评论（若 API 支持）。（进行中：已完成登录后分类/分区列表 + 搜索 + 详情 + 阅读器MVP（含图片渲染与进度持久化））
- [ ] 实现阅读器 MVP：图片分页加载、进度保存、章节切换、失败重试。
- [ ] 实现本地功能：收藏、历史、离线缓存入口。
- [ ] 实现设置页：阅读偏好、缓存管理、网络策略。

## 5. 漫画阅读模块专项（重点）
- [ ] 调研并选定一个可参考的开源阅读器实现思路（仅借鉴架构与交互）。
- [ ] 设计阅读数据流：章节 -> 图片列表 -> 预取队列 -> 渲染。
- [ ] 优化大图体验：分辨率自适应、内存回收、滑动流畅性、占位图策略。
- [ ] 加入容错：单页失败重试、整章失败回退、断网提示与恢复。

## 6. 测试与质量保障
- [ ] 单元测试：签名算法、Repository、分页逻辑、阅读进度存储。
- [ ] 接口测试：关键 API 的成功/失败分支（必要时 Mock）。
- [ ] UI 测试：登录、搜索、阅读流程的基础用例。
- [ ] 兼容性测试：Android 7/8/10/12/14/16 关键路径验证。
- [ ] 性能测试：首屏时间、滚动掉帧、阅读内存峰值、网络耗时分析。

## 7. 发布与维护
- [ ] 生成发布构建配置（签名、混淆、资源压缩、崩溃上报）。
- [ ] 输出使用说明与风险声明（第三方 API 可用性、账号安全提醒）。
- [ ] 制定后续迭代清单（下载、同步、多源扩展、无障碍优化）。

## 8. 里程碑建议
- [ ] M1（基础可用）：登录 + 首页 + 搜索 + 详情。
- [ ] M2（核心体验）：阅读器可用 + 阅读进度 + 收藏历史。
- [ ] M3（完善阶段）：缓存管理 + 设置页 + 兼容性优化 + 测试补齐。

---
如你确认这份清单，我下一步将按此顺序正式开工（先做“接口梳理 + 架构骨架”）。
